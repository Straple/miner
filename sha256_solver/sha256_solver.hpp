#pragma once

#include <string>

// принимает строку из байтов
// последний байт является X
// мы ищем лучший X, чтобы получить как можно меньший хеш
// возвращаем этот X и hash
//std::pair<uint32_t, std::string> find_best_hash(const std::string &bytes);
